version: 2

sources:
  - name: eth
    description: "This is the source for the ETH project."
    database: eth
    schema: eth
    tables:
      - name: eth_transactions
        description: "Table containing Ethereum transaction data."
        freshness:
          warn_after: {count: 1, period: hour}
          error_after: {count: 3, period: hour}
        loaded_at_field: BLOCK_TIMESTAMP
